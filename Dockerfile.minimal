# Dockerfile ultra-simplificado sin debugging
FROM mcr.microsoft.com/dotnet/aspnet:8.0

WORKDIR /app
COPY publish/ ./

ENV ASPNETCORE_ENVIRONMENT=Production

# Comando directo sin debugging
ENTRYPOINT ["dotnet", "citas.dll", "--urls", "http://0.0.0.0:$PORT"]
